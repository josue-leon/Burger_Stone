import 'package:flutter/cupertino.dart';
//para establecer color a la aplicacion
class MyColors{
    static Color primaryColor = Color(0xFFFE3648);
    static Color primaryOpacityColor = Color.fromRGBO(254, 54, 72, 0.09);
    static Color primaryColorDark = Color(0xFF000000);
}